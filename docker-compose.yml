services:
  mysql-persona:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: db_persona
    ports:
      - "3309:3306"
    volumes:
      - mysql_persona_data:/var/lib/mysql

volumes:
  mysql_persona_data:
